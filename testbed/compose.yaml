services:
  grocy:
    image: lscr.io/linuxserver/grocy:4.4.1
    container_name: grocy-mcp-testbed
    ports:
      - "9283:80"
    environment:
      PUID: "1000"
      PGID: "1000"
      TZ: "UTC"
    volumes:
      - ./runtime/grocy-data:/config
    restart: unless-stopped

